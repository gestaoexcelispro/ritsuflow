import { NextResponse } from 'next/server'
import { createClient } from '@supabase/supabase-js'

export const dynamic='force-dynamic'
function adminClient(){const url=process.env.NEXT_PUBLIC_SUPABASE_URL;const serviceKey=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!url||!serviceKey)throw new Error('Server user-management configuration is incomplete. Add SUPABASE_SERVICE_ROLE_KEY to the server environment.');return createClient(url,serviceKey,{auth:{autoRefreshToken:false,persistSession:false}})}
function authClient(token){const url=process.env.NEXT_PUBLIC_SUPABASE_URL;const key=process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY;if(!url||!key)throw new Error('Supabase public configuration is incomplete.');return createClient(url,key,{global:{headers:{Authorization:`Bearer ${token}`}},auth:{autoRefreshToken:false,persistSession:false}})}
export async function POST(request){try{const authorization=request.headers.get('authorization')||'';const token=authorization.startsWith('Bearer ')?authorization.slice(7):'';if(!token)return NextResponse.json({error:'Authentication required.'},{status:401});const body=await request.json();const organizationId=String(body.organizationId||'');const fullName=String(body.fullName||'').trim();const email=String(body.email||'').trim().toLowerCase();const jobTitle=String(body.jobTitle||'').trim();const role=String(body.role||'member');const projectAccessMode=String(body.projectAccessMode||'assigned_projects');const projectIds=Array.isArray(body.projectIds)?body.projectIds.filter(Boolean):[];const workspaceAccess=Array.isArray(body.workspaceAccess)?body.workspaceAccess.filter(Boolean):[];const avatarExt=['jpg','jpeg','png','webp'].includes(String(body.avatarExt||'').toLowerCase())?String(body.avatarExt).toLowerCase():null;if(!organizationId||!fullName||!email)return NextResponse.json({error:'Name, email and organization are required.'},{status:400});if(!['owner','admin','manager','member','viewer'].includes(role))return NextResponse.json({error:'Invalid RitsuFlow role.'},{status:400});if(!['all_projects','assigned_projects'].includes(projectAccessMode))return NextResponse.json({error:'Invalid project access mode.'},{status:400});if(projectAccessMode==='assigned_projects'&&!projectIds.length)return NextResponse.json({error:'Select at least one assigned project.'},{status:400});if(!workspaceAccess.length)return NextResponse.json({error:'Select at least one workspace.'},{status:400});const caller=authClient(token);const {data:canManage,error:permissionError}=await caller.rpc('can_manage_organization_users',{target_organization_id:organizationId});if(permissionError||!canManage)return NextResponse.json({error:'You do not have permission to manage users for this organization.'},{status:403});const admin=adminClient();if(projectIds.length){const {data:validProjects,error:projectError}=await admin.from('projects').select('id').eq('organization_id',organizationId).in('id',projectIds);if(projectError)throw projectError;if((validProjects||[]).length!==projectIds.length)return NextResponse.json({error:'One or more selected projects do not belong to this organization.'},{status:400})}const {data:licenseRows}=await caller.rpc('get_platform_organizations');const license=Array.isArray(licenseRows)?licenseRows.find(x=>x.organization_id===organizationId):null;const {count:activeCount,error:countError}=await admin.from('organization_members').select('user_id',{count:'exact',head:true}).eq('organization_id',organizationId).eq('status','active');if(countError)throw countError;if(license?.seat_limit!=null&&activeCount>=license.seat_limit)return NextResponse.json({error:`Seat limit reached (${license.seat_limit}). Increase the license seat limit before activating another user.`},{status:409});const redirectTo=`${new URL(request.url).origin}/login`;const {data:invited,error:inviteError}=await admin.auth.admin.inviteUserByEmail(email,{redirectTo,data:{full_name:fullName,job_title:jobTitle,organization_id:organizationId,workspace_access:workspaceAccess}});if(inviteError)return NextResponse.json({error:inviteError.message},{status:400});const invitedUser=invited?.user;if(!invitedUser?.id)throw new Error('Supabase did not return the invited user.');const avatarPath=avatarExt?`${organizationId}/${invitedUser.id}/avatar.${avatarExt}`:null;const {error:profileError}=await admin.from('profiles').upsert({id:invitedUser.id,email,full_name:fullName},{onConflict:'id'});if(profileError)throw profileError;const {error:userProfileError}=await admin.from('user_profiles').upsert({user_id:invitedUser.id,email,full_name:fullName,display_name:fullName,job_title:jobTitle,status:'invited',avatar_path:avatarPath},{onConflict:'user_id'});if(userProfileError)throw userProfileError;const {error:accessError}=await admin.rpc('set_organization_member_access',{target_organization_id:organizationId,target_user_id:invitedUser.id,target_role:role,target_project_access_mode:projectAccessMode,target_status:'invited',target_project_ids:projectAccessMode==='assigned_projects'?projectIds:[]});if(accessError)throw accessError;let avatarUpload=null;if(avatarPath){const {data:signed,error:signedError}=await admin.storage.from('user-avatars').createSignedUploadUrl(avatarPath);if(signedError)throw signedError;avatarUpload={path:avatarPath,token:signed.token}}return NextResponse.json({ok:true,user:{id:invitedUser.id,email,fullName,jobTitle,role,status:'invited',projectAccessMode,projectIds,workspaceAccess,avatarPath},avatarUpload})}catch(error){console.error('Invite organization user failed:',error);return NextResponse.json({error:error?.message||'Unable to invite user.'},{status:500})}}
